# IMDb user ID used to populate the default Stremio catalog
DEFAULT_IMDB_USER_ID=ur31595220

# TMDB API key for fetching movie/series posters and metadata
TMDB_API_KEY=your_tmdb_api_key_here

# Secret token for authenticating cron job requests (GitHub Actions)
CRON_SECRET=your_secure_random_secret_here

# Production environment (automatically set by Vercel)
NODE_ENV=production

# Optional: Vercel app URL (set automatically by Vercel)
VERCEL_URL=your-app.vercel.app
